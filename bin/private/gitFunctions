#!/bin/bash
#=========================================================================
# Copyright (c) 2015 GemTalk Systems, LLC <dhenrich@gemtalksystems.com>.
#=========================================================================

updateClone() {
  master="$1"
  workingDir="$2"
  theBranchName=`git --work-tree="$workingDir" rev-parse --abbrev-ref HEAD`
  git --work-tree="$workingDir" checkout $master
  git --work-tree="$workingDir" pull origin $master
  git --work-tree="$workingDir" checkout $theBranchName
  git --work-tree="$workingDir" merge $master
}

